services:
  astro:
    build: .
    image: astro-builder:latest
    ports:
      - "3003:3000"
    environment:
      - PORT=3000
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npm install --no-audit --silent && npm run dev -- --host 0.0.0.0 --port $$PORT"
