---
import Layout from "../../layouts/Layout.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import { Icon } from "astro-icon/components";
import { SITE } from "../../config/site.mjs";
---

<Layout
  title={`shishutsukan | ${SITE.title}`}
  description="支出館：一覧性を重視した支出管理アプリ"
  canonicalURL={`${SITE.url}/apps/shishutsukan`}
>
  <Header />
  <main id="main-content" class="mx-auto max-w-5xl px-6 py-16">
    <header class="mb-10">
      <p class="text-sm font-semibold text-gray-500">App</p>
      <h1 class="mt-2 text-4xl font-bold text-gray-900">shishutsukan</h1>
      <p class="mt-4 text-lg text-gray-600">
        支出館：一覧性を重視した支出管理アプリ
      </p>
      <div class="mt-6 flex flex-wrap gap-3">
        <a
          href="https://github.com/sfujibijutsukan/shishutsukan"
          class="inline-flex items-center gap-2 rounded-lg border border-gray-300 bg-white px-4 py-2 text-gray-900 hover:bg-gray-50 transition-colors"
          target="_blank"
          rel="noopener noreferrer"
        >
          <Icon name="tabler:brand-github" class="h-5 w-5" aria-hidden="true" />
          GitHub
        </a>
      </div>
    </header>

    <section class="grid gap-8 lg:grid-cols-[2fr,1fr]">
      <div class="space-y-8">
        <div class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm">
          <h2 class="text-2xl font-semibold text-gray-900">概要</h2>
          <p class="mt-3 text-gray-600">
            日々の支出をジャンル別に記録し、月別・日別でグラフ化。
            直感的な入力と一覧性を両立し、支出の見える化をシンプルに行えます。
          </p>
        </div>

        <div class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm">
          <h2 class="text-2xl font-semibold text-gray-900">UI</h2>
          <p class="mt-3 text-gray-600">
            一覧性・操作性を重視したシンプルなUI設計。
          </p>
        </div>

        <div class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm">
          <h2 class="text-2xl font-semibold text-gray-900">操作画面</h2>
          <div class="mt-4 overflow-hidden rounded-xl border border-gray-200 bg-gray-50">
            <img
              src="/images/apps/shishutsukan.png"
              alt="支出館の画面"
              class="h-full w-full object-cover"
            />
          </div>
        </div>
      </div>

      <aside class="space-y-6">
        <div class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm">
          <h2 class="text-xl font-semibold text-gray-900">技術スタック</h2>
          <ul class="mt-4 space-y-2 text-gray-600">
            <li>フロントエンド: React + recharts + react-datepicker</li>
            <li>バックエンド: FastAPI + SQLite</li>
            <li>データベース: SQLite（支出データ・ジャンル管理）</li>
            <li>REST API による端末間データ同期</li>
            <li>Docker Compose対応</li>
          </ul>
        </div>
      </aside>
    </section>
  </main>
  <Footer />
</Layout>
